# 任务日志 — RLVM Android 封装

> 仓库：https://github.com/Tooke258/rlvm-android
> 时间跨度：2026-09-19 → 2026-09-20
> 上游：RLVM 0.14（GPLv3）｜本项目许可：GPLv3
> 结果：v0.1.0 已发布，真机验证 Kud Wafter 可玩（标题、正文、名字、语音、BGM、存档、Config）

## 0. 任务定义

把 RLVM（RealLive 虚拟机）封装成现代 Android 应用：

| 项 | 要求 |
| --- | --- |
| minSdk / targetSdk | 31 / 35 |
| ABI | arm64-v8a、armeabi-v7a |
| 原生层 | NDK + CMake + C++17 |
| JNI | 显式 `JNIRegisterNatives`，不用动态查找 |
| 文件访问 | Storage Access Framework，不用 File 路径直读 |
| 音频 | AAudio |
| 渲染 | OpenGL ES 3.0 |

原则：**不打包任何商业游戏资源**；应用指向游戏的**根目录**（不是 exe），
不修改上游 `libreallive` / `machine` / `modules` / `encodings` 的解析与指令语义。

## 1. 起点：一块"装了却找不到"的环境（09-19）

用户重装过系统、文件保留。现象：JDK / Android SDK / NDK / Gradle / CMake 全都
不在 PATH 上。

排查结论：注册表与 `C:\Program Files` 被重置，用户环境变量与 PATH 清空；非系统盘
上的 Android SDK、某个第三方软件自带的 JDK 21，以及 `<用户目录>` 下的 Gradle
缓存（760 MB 依赖 + Gradle 9.1.0 发行版）幸存。

处理与验证：

1. 写入**用户级永久环境变量**（`JAVA_HOME`、`ANDROID_SDK_ROOT`、`ANDROID_HOME`、
   `ANDROID_NDK_HOME`、`ANDROID_NDK_ROOT`、`HOME`，并追加 PATH）；
2. `gradle --version` 通过（Gradle 9.1.0 + JVM 21.0.9）；
3. NDK r28c 交叉编译两个 ABI 出 `.so`，ELF 头 `ELF64/DYN/AArch64`，LOAD 段
   `0x4000` 对齐（满足 Android 15+ 的 16 KB 页要求）；
4. `git init` 基线提交，跟踪 1066 个文件。

同时确定了贯穿全程的一条工作约束：**沙箱把 `.git` 与 `~/.gradle` 设为只读**，
所以构建与提交一律提权（沙箱外）执行。

## 2. 阶段 1：把 RLVM 核心编进 Android

- 上游源码保留在 `rlvm-release-0.14/`，**不下沉、不拷贝**，CMake 直接引用其
  149 个编译单元。
- 依赖取舍：核心 149 个文件完全不含 `<SDL/...>`，但深度依赖 Boost
  （`BOOST_CLASS_VERSION` ×9、`boost::serialization::access` ×24、文本归档 ×72），
  存档格式离不开它 → 从源码直接编 Boost.Filesystem / Serialization / Iostreams，
  绕开 b2。
- 补 libogg / libvorbis 的编解码部分；给 vendored `wavfile.cc` 补一个最小的
  `<SDL/SDL_mixer.h>` shim。
- 验收：两个 ABI 都产出 `librlvm.so`。

## 3. 阶段 2：文件访问

- 抽象出 `game_file_system`：普通路径后端 + SAF 后端，大小写不敏感解析文件名。
- `SEEN####.TXT` 的场景覆盖也走文件系统层。
- 真机验证：`GAMEEXE.INI` 解析、`SEEN.TXT` 经 fd 打开、TOC 解析出 62 个场景。

## 4. 阶段 3：画面

- 架构：图像解码（PDT / G00 / BMP）→ CPU 合成 → OpenGL ES 3.0 呈现。
- **真实游戏首屏全黑**的根因是 `AndroidSurface` 没处理 GRP type-2 的区域表，
  这个 bug 在最小夹具上永远不会暴露。
- 后处理：把逐像素工作移出渲染热路径，天玑 1200 上效率恢复正常。

## 5. 阶段 4：声音

| 现象 | 根因 | 处理 |
| --- | --- | --- |
| 音调偏高约 8.8% | 上游把 44.1 kHz 源按 48 kHz 喂给转换器 | 加 Lanczos-2 重采样 |
| 起播必有噪点 / 爆音 | NWA 首块脏帧 + 无起播包络 | 丢弃首块脏帧 + 起播淡入 |
| 声音结束被硬截断 | 环形缓冲被直接丢弃 | 停播淡出、先排空再收尾 |
| 换源有咔哒声 | 两个源瞬间切换 | 交叉淡化 |
| 人声发闷、高音破音 | 增益结构与限幅缺失 | 软限幅 + 主增益重调 |
| SAF 下没有语音 | 语音归档走的是旧路径 | KOE / NWK / OVK 改走游戏文件系统 |

后端用 AAudio，不引 Oboe（minSdk 31 > API 26）。

## 6. 文字与名字框

- 用 FreeType + 系统 CJK 字体渲染；一度整屏文字全黑，原因是 `font_colour_`
  没有取到默认值。
- "上下不齐"是因为按字形顶部对齐 → 改为按 FreeType bearing 做**基线**排版，
  字距与基线随后调平。
- 角色名不显示：按名字实际宽度给名字 surface 定尺寸后正常。

## 7. 交互

- 单击 = 鼠标左键；**长按 400 ms = 鼠标右键**（对应游戏自己的右键菜单，
  否则只能靠杀进程退出）。这条已写进中英双版 README。
- 触摸坐标映射与画面适配共用同一套 `fitScale()`，四种适配模式下点击都对齐。

## 8. 存档与 Config

- 写路径分两步落地：先加平台写钩子，再补 Android / SAF 实现。
- 存档落在应用私有目录 `Android/data/org.rlvm.android/files/.rlvm/KEY_<regname>/`。
  中间走过一次弯路（把存档目录指到游戏目录），因目录枚举没接上而后回退。
- Config 菜单读写的三个 opcode 从 `unsupported` 改为真实实现，并持久化进
  `SystemGlobals`（`BOOST_CLASS_VERSION` 1 → 2，`CURRENT_GLOBAL_VERSION` 3 → 4）。
- 一度出现"同进程内能保留、换进程就丢"，最终确认存档与 Config 的读写都通。

## 9. 界面

悬浮球（可拖动、限位、贴边）+ 侧栏（用 `VISIBLE` / `GONE` 切换，避免 SurfaceView
残留区域）、横竖屏切换、沉浸式状态栏、四种画面适配（适配 / 左右贴边 / 上下贴边 /
拉伸）。

## 10. 汉化：查清了，但没接入

- 译文不在 `SEEN.TXT`（那是日文原版），也不在汉化补丁的 exe 里（只有 14 条
  Config 界面串）。
- 真正的译文在汉化补丁进程的**内存转储**里：2398 条 GBK 中文串，含剧本正文。
  为此写了转储解析工具与 IDA 无头分析脚本，抽出的译文只做**本地留档**，
  不进仓库。
- 结论：该补丁是运行时加载器。后续决定改用 LLM 翻译，此路线暂停。
- **当前版本读的是日文原版文本。**

## 11. 发布

- 准备工作：应用图标（用户提供的素材，含自适应图标各尺寸）、中英双版 README
  （含上游支持游戏表）、GPLv3 全文与上游许可总览。
- **v0.1.0**：Release 附 `app-release.apk`，13,844,026 字节，
  arm64-v8a + armeabi-v7a，minSdk 31 / targetSdk 35，APK Signature Scheme v2。
- 公开前去隐私：
  - 入库文件里的绝对路径、设备序列号、主机名 / 账户名全部换成占位符
    （`<工作区>`、`<Android SDK 路径>`、`<Boost 源码树>`、`<设备序列号>` …）；
  - 本机真值集中留在 `local-data/LOCAL-PATHS.md`（该目录 gitignore，不进仓库）；
  - `.vscode/c_cpp_properties.json`（纯本机 IntelliSense 配置）移出版本控制；
  - CMake 的 Boost 定位从硬编码路径改为 `-DBOOST_SOURCE_DIR=` > 环境变量 >
    `local.properties` 的 `boost.dir=` > `third_party/` 解包目录，并实测全新
    configure 仍能解析到 Boost。
- 密钥与口令（`rlvm-release.jks`、`keystore.properties`）、内存转储、译文数据
  从来**没有被提交过**，始终在 gitignore 之内。

## 12. 已知限制

- PC 版 RealLive 的原生存档格式与本应用不互通（未做格式转换）；
- 汉化补丁未接入，当前读日文原版；
- HIK 渲染未接入，用到 HIK 的作品会缺特效；
- KOE 以外的语音归档只有部分作品验证过；
- 仍有部分未实现的 opcode。

## 13. 经验

- **夹具证明"链路通"，不证明"游戏能跑"。** 真实游戏一上来就暴露了 4 个夹具
  掩盖的 bug（首屏全黑、语音找不到、文字全黑等）。
- 沙箱内 `.git` 与 `~/.gradle` 只读 → 构建与 git 写操作必须提权，这是常态。
- Windows PowerShell 5.1 按 ANSI 读无 BOM 的 `.ps1`，因此仓库里的脚本一律
  写成纯 ASCII。
- 提交信息含引号 / 尖括号时用 `git commit -F <文件>`，别塞进命令行。
- 诊断开关（`rlvm-diag.txt`）做成免重新构建的运行时参数，排查迭代快很多。

## 14. 构建与验证入口

```powershell
# 环境（本机路径见 local-data/LOCAL-PATHS.md）
$env:JAVA_HOME='<JDK 21 路径>'
$env:ANDROID_SDK_ROOT='<Android SDK 路径>'

# 第三方依赖（FreeType / libogg / libvorbis）
powershell -File tools/setup_third_party.ps1

# 构建
.\gradlew.bat assembleDebug     # debug：多编一个 x86_64，供模拟器
.\gradlew.bat assembleRelease   # release：只含 arm64-v8a 与 armeabi-v7a
```

真机测试回路、诊断开关、测试夹具见 `docs/TESTING.md`；
架构、决策记录、环境记录见 `docs/ARCHITECTURE.md`、`docs/DECISIONS.md`、
`docs/ENVIRONMENT.md`。
